import "dotenv/config"; const t=process.env.BOT_TOKEN, p=process.env.WC_PROJECT_ID; console.log("env -> token:", t?("yes, id "+t.split(":")[0]):"MISSING", "| projectId:", p?(p.length+" chars"):"MISSING", "| owner:", process.env.OWNER_ID||"MISSING"); const to=(pr,ms)=>Promise.race([pr,new Promise((_,r)=>setTimeout(()=>r(new Error("TIMED OUT after "+ms/1000+"s")),ms))]); try{const r=await to(fetch("https://api.telegram.org/bot"+t+"/getMe"),12000);const j=await r.json();console.log("telegram ->", j.ok?("OK, @"+j.result.username):("REJECTED: "+j.description))}catch(e){console.log("telegram -> FAIL:",e.message)} try{await to(new Promise((res,rej)=>{const s=new WebSocket("wss://relay.walletconnect.org/?projectId="+p);s.onopen=()=>{s.close();res()};s.onerror=()=>rej(new Error("refused"));s.onclose=e=>{if(e.code!==1000)rej(new Error("closed, code "+e.code))}}),12000);console.log("relay -> OK")}catch(e){console.log("relay -> FAIL:",e.message)} process.exit(0);
